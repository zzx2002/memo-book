-- ============================================================
--  v0.4：提醒提前量
-- ============================================================

-- 在 remind_at 的基础上提前多少分钟提醒（0 = 准时）
ALTER TABLE tasks ADD COLUMN remind_before INTEGER NOT NULL DEFAULT 0;
